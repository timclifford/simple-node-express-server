version: 2
jobs:
  build:
    docker:
      — image: circleci/node:10
    steps:
      - checkout
      - run: yarn install

workflows:
  version: 2
  build:
    jobs:
      - 'node-10'
      - 'node-latest'

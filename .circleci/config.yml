version: 2
jobs:
  build:
    working_directory: ~/simple-node-js
    docker:
      - image: circleci/node:16.13.1
    steps:
      - checkout
      - run:
          name: install
          command: yarn install
